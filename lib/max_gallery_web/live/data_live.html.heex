<!DOCTYPE html>
<html lang="pt-BR">
    <header class="bg-gray-800 text-white p-1 flex items-center">
        <.form for={%{}} action={~p"/"} method="get">
            <button>
                <img src="/images/logo.png" alt="Logo" class="w-auto h-14 px-2">
            </button>
        </.form>
    </header>


    <body>
        <div class="grid grid-cols-3 gap-4 p-4">
            <%= for item <- @datas do %>

                <div class="border rounded-xl shadow-md p-4 bg-white grid grid-rows-[1fr_auto] h-40">
                    <span class="overflow-hidden break-all self-center text-center font-semibold">
                        <%= item.name %>
                    </span>

                    <div class="flex justify-center gap-2 mt-2">
                        <button class="text-blue-600 hover:underline">Show</button>
                        <button class="text-yellow-600 hover:underline">Edit</button>
                        <button class="text-red-600 hover:underline">Delete</button>
                    </div>
                </div>

            <% end %>
        </div>
    </body>

</html>
