<!DOCTYPE html>
<html lang="en">
  <body>
    <div class="flex justify-center items-center h-[70vh]">
      <div class="flex flex-col items-center h-[60%] w-[60%] rounded-lg shadow-xl m-auto border p-8">
        <h1 class="text-2xl font-bold">Reset your Password</h1>
        <p class="text-lg text-gray-600 mb-8">
          Insert your new password for the "{@email}" user.
        </p>

        <.form
          for={%{}}
          method="post"
          action="/reset-passwd"
          class="flex flex-col h-full w-full items-center justify-end mb-12"
        >
          <input
            name="new_passwd"
            type="password"
            placeholder="Jonh_Smith200"
            class="px-6 py-2 w-[50%] border border-gray-600 border-1 focus:border-blue-600 rounded-lg text-center mb-1"
            required
          />

          <input type="hidden" name="email" value={@email} />

          <button type="submit" class="px-6 py-2 w-[50%] text-white bg-blue-600 rounded-lg">
            Confirm
          </button>
          <p class="text-sm text-red-600 text-center">{@err}</p>
        </.form>
      </div>
    </div>
  </body>
</html>
